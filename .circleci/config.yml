version: 2.1
jobs:
  test:
    docker:
      - image: cimg/go:1.24
    steps:
      - checkout
      - run:
          name: Run tests
          command: go test ./...
workflows:
  test:
    jobs:
      - test
